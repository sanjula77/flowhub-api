FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (development dependencies included)
RUN npm install

# Expose port
EXPOSE 3001

# Start dev server with watch mode (will be overridden by docker-compose command)
CMD ["npm", "run", "start:dev"]

